# Use the official Channels DVR image
FROM fancybits/channels-dvr:latest

# Set working directory (optional)
WORKDIR /channels-dvr

# Expose DVR web interface
EXPOSE 8089

# Default command (inherited from the image)
CMD ["channels-dvr"]
